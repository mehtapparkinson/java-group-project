spring:
  application:
    name: java-group-project
  datasource:
    url: jdbc:mysql://localhost:3307/MovieTrackerDB
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
